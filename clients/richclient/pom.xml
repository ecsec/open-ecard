<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
	<groupId>org.openecard.client</groupId>
	<artifactId>clients</artifactId>
	<version>1.0.0-pre2-SNAPSHOT</version>
    </parent>

    <groupId>org.openecard.client.clients</groupId>
    <artifactId>richclient</artifactId>
    <packaging>jar</packaging>
    <name>Rich Client</name>

    <build>
	<plugins>
	    <plugin>
		<artifactId>maven-assembly-plugin</artifactId>
		<version>2.3</version>
		<configuration>
		    <descriptorRefs>
			<descriptorRef>jar-with-dependencies</descriptorRef>
		    </descriptorRefs>
		    <archive>
			<manifest>
			    <mainClass>org.openecard.client.richclient.RichClient</mainClass>
			</manifest>
		    </archive>
		</configuration>
		<executions>
		    <execution>
			<id>make-assembly</id>
			<phase>package</phase>
			<goals>
			    <goal>single</goal>
			</goals>
		    </execution>
		</executions>
	    </plugin>
	</plugins>
    </build>


    <properties>
	<wininstall.resources.dir>${basedir}/installer/windows</wininstall.resources.dir>
	<wininstall.tmp.dirname>wix-tmp</wininstall.tmp.dirname>
	<wininstall.tmp.dir>${project.build.directory}/${wininstall.tmp.dirname}</wininstall.tmp.dir>
    </properties>

    <profiles>
	<profile>
	    <id>windows-installer</id>
	    <build>
		<plugins>
		    <plugin>
			<groupId>org.apache.maven.plugins</groupId>
			<artifactId>maven-antrun-plugin</artifactId>
			<version>1.7</version>
			<executions>
			    <execution>
				<id>antrun</id>
				<phase>package</phase>
				<configuration>
				    <target>
					<property name="version" value="${project.build.version}" />
					<copy todir="${wininstall.tmp.dir}" file="${wininstall.resources.dir}/Open-eCard-Logo.ico" />
					<copy todir="${wininstall.tmp.dir}" file="${wininstall.resources.dir}/run.bat" />
					<copy todir="${wininstall.tmp.dir}/lib" flatten="true" >
					    <path>
						<path refid="maven.runtime.classpath" />
						<pathelement location="${project.build.directory}/${project.build.finalName}.${project.packaging}" />
					    </path>
					</copy>


					<!-- build dependency string for replacement in WIX configuration file -->
					<fileset id="alljars.fileset" dir="${wininstall.tmp.dir}/lib" includes="*.jar" />
					<pathconvert pathsep=";" property="alljars.property" refid="alljars.fileset">
					    <mapper type="flatten" />
					</pathconvert>
					<echo file="${wininstall.tmp.dir}/tmp.txt">${alljars.property}</echo>
					<replaceregexp file="${wininstall.tmp.dir}/tmp.txt" match="\A" replace="&lt;File Source='lib/"/>
					<replaceregexp file="${wininstall.tmp.dir}/tmp.txt" match=";" replace="'/>&lt;File Source='lib/" flags="g" />
					<replaceregexp file="${wininstall.tmp.dir}/tmp.txt" match="\Z" replace="'/>"/>
					<loadfile property="all_jars" srcfile="${wininstall.tmp.dir}/tmp.txt" />


					<!-- build version number string for replacement in WIX configuration file -->
					<echo file="${wininstall.tmp.dir}/version_number.txt">${version}</echo>
					<replaceregexp file="${wininstall.tmp.dir}/version_number.txt" match="((\d)*(\.(\d)*){0,3})(.*)" replace="\1"/>
					<loadfile property="version_number" srcfile="${wininstall.tmp.dir}/version_number.txt" />


					<copy tofile="${wininstall.tmp.dir}/WindowsInstaller.wxs" file="${wininstall.resources.dir}/WindowsInstaller.wxs.xml" >
					    <filterset>
						<filter token="ALL_JARS" value="${all_jars}"/>
						<filter token="VERSION" value="${version}"/>
						<filter token="VERSION_NUMBER" value="${version_number}"/>
					    </filterset>
					</copy>
				    </target>
				</configuration>
				<goals>
				    <goal>run</goal>
				</goals>
			    </execution>
			</executions>
		    </plugin>
		    <plugin>
			<groupId>org.apache.npanday.plugins</groupId>
			<artifactId>wix-maven-plugin</artifactId>
			<version>1.4.0-incubating</version>
			<configuration>
			    <sourceFiles>
				<sourceFile>${wininstall.tmp.dir}/WindowsInstaller.wxs</sourceFile>
			    </sourceFiles>
			    <outputDirectory>${wininstall.tmp.dir}</outputDirectory>
			    <objectFiles>
				<objectFile>${wininstall.tmp.dir}/WindowsInstaller.wixobj</objectFile>
			    </objectFiles>
			    <outputFile>${project.build.directory}/${project.build.finalName}.msi</outputFile>
			</configuration>
			<executions>
			    <execution>
				<id>wix</id>
				<phase>package</phase>
				<goals>
				    <goal>candle</goal>
				    <goal>light</goal>
				</goals>
			    </execution>
			</executions>
		    </plugin>
		    <plugin>
			<groupId>org.apache.maven.plugins</groupId>
			<artifactId>maven-clean-plugin</artifactId>
			<version>2.4.1</version>
			<executions>
			    <execution>
				<id>auto-clean</id>
				<phase>package</phase>
				<goals>
				    <goal>clean</goal>
				</goals>
				<configuration>
				    <excludeDefaultDirectories>true</excludeDefaultDirectories>
				    <filesets>
					<fileset>
					    <directory>${wininstall.tmp.dir}</directory>
					    <includes>
						<include>**</include>
					    </includes>
					</fileset>
					<fileset>
					    <directory>${project.build.directory}</directory>
					    <includes>
						<include>${wininstall.tmp.dirname}</include>
					    </includes>
					</fileset>
					<fileset>
					    <directory>${project.build.directory}</directory>
					    <includes>
						<include>${project.build.finalName}.wixpdb</include>
					    </includes>
					</fileset>
				    </filesets>
				</configuration>
			    </execution>
			</executions>
		    </plugin>
		</plugins>
	    </build>
	</profile>
    </profiles>


    <dependencies>
	<dependency>
	    <groupId>org.openecard.client.sal</groupId>
	    <artifactId>tiny-sal</artifactId>
	    <version>${project.version}</version>
	</dependency>
	<dependency>
	    <groupId>org.openecard.client.transport</groupId>
	    <artifactId>dispatcher</artifactId>
	    <version>${project.version}</version>
	</dependency>
	<dependency>
	    <groupId>org.openecard.client.ifd.protocols</groupId>
	    <artifactId>pace</artifactId>
	    <version>${project.version}</version>
	</dependency>
	<dependency>
	    <groupId>org.openecard.client.sal.protocols</groupId>
	    <artifactId>eac</artifactId>
	    <version>${project.version}</version>
	</dependency>
	<dependency>
	    <groupId>org.openecard.client.management</groupId>
	    <artifactId>tiny-management</artifactId>
	    <version>${project.version}</version>
	</dependency>
	<dependency>
	    <groupId>org.openecard.client.transport</groupId>
	    <artifactId>tls</artifactId>
	    <version>${project.version}</version>
	</dependency>
	<dependency>
	    <groupId>org.openecard.client.transport</groupId>
	    <artifactId>paos</artifactId>
	    <version>${project.version}</version>
	</dependency>
	<dependency>
	    <groupId>org.openecard.client</groupId>
	    <artifactId>event-manager</artifactId>
	    <version>${project.version}</version>
	</dependency>
	<dependency>
	    <groupId>org.openecard.client.gui</groupId>
	    <artifactId>swing</artifactId>
	    <version>${project.version}</version>
	</dependency>
	<dependency>
	    <groupId>org.openecard.client.clients</groupId>
	    <artifactId>client-connector</artifactId>
	    <version>${project.version}</version>
	</dependency>
	<dependency>
	    <groupId>ch.qos.logback</groupId>
	    <artifactId>logback-classic</artifactId>
	    <version>1.0.3</version>
	</dependency>
    </dependencies>

</project>
