compile:
  stage: compile
  image: public.docker.ecsec.de/ecsec/tools/cimg-android-jdk25:2025.12
  #image: docker.io/cimg/android:2025.10
  tags:
    - "mac"
  cache:
    - key: "$OEC_JAVA_CACHE_KEY"
      paths: !reference [ .cache-gradle-base, paths ]
      policy: pull-push
    - key: "$OEC_JAVA_TMP_CACHE_KEY"
      paths: [ .cache-gradle-builds, paths ]
      policy: pull-push
  script:
    - !reference [.versioning, script]
    - ANDROID_HOME=/Users/florianotto/android-sdk-root/ ./gradlew assemble
